import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  paper: {
    margin: "auto",
    width: "30em",
  },
  verticalGrid: {
    height: "20em",
  },
  mainContent: {
    height: "15em",
  },
  continueButton: {
    margin: "2em 0 3em 0",
  },
}));

export default useStyles;
