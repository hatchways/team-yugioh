import React, { useState } from "react";
import PropTypes from "prop-types";
import { makeStyles } from "@material-ui/core";

const IndividualEvent = ({ key }) => {
  console.log(key);
  return <div>hi</div>;
};

const useStyles = makeStyles((theme) => ({}));

export default IndividualEvent;
